import { InnerHtmlObserverDirective } from './inner-html-observer.directive';

describe('InnerHtmlObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new InnerHtmlObserverDirective();
    expect(directive).toBeTruthy();
  });
});
